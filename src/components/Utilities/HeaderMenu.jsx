const HeaderMenu = ({ title }) => {
  return (
    <div>
      <div className="headerPopuler">
        <h3 className="text-center text-3xl text-primary">{title}</h3>
      </div>
    </div>
  );
};

export default HeaderMenu;
